# Skill: Example

This is an example skill to demonstrate the skill system.

## What this skill does

When the user asks about the current time or date, you can answer directly.
When the user asks to run a shell command, you can describe what it would do.

## Example commands

- "What time is it?" → answer with current time knowledge
- "List files" → describe ls command behavior

## Notes

- Replace this with your own SKILL.md files
- Each skill should be in its own subdirectory: `skills/<name>/SKILL.md`
- Claude will automatically use relevant skills based on the user's request
